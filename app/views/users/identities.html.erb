<div class="page-header">
	<h1>Your social networks</h1>
</div>
<div class="panel panel-default">
	<div class="panel-body">
		Currently, linking other social networks will only allow you to login with them.<br />
		But in the future (and we will ask for more permissions when the time comes) you will be able to post to them on certain actions (like when you finish a game).
	</div>
</div>
<div class="row">
	<div class="col-md-4">
		<% if @identities['google_oauth2'] %>
			<%= link_to "#", class: 'btn btn-lg btn-social-icon btn-google disabled', title: "Authorize Google", rel: "nofollow" do %>
				<i class="fa fa-google"></i>
			<% end %>
			Connected
		<% else %>
			<%= link_to user_google_oauth2_omniauth_authorize_path, class: 'btn btn-lg btn-social-icon btn-google', title: "Authorize Google", rel: "nofollow" do %>
				<i class="fa fa-google"></i>
			<% end %>
			Connect
		<% end -%>
	</div>
	<div class="col-md-4">
		<% if @identities['facebook'] %>
			<%= link_to "#", class: 'btn btn-lg btn-social-icon btn-facebook disabled', title: "Authorize Facebook", rel: "nofollow" do %>
				<i class="fa fa-facebook"></i>
			<% end %>
			Connected
		<% else %>
			<%= link_to user_facebook_omniauth_authorize_path, class: 'btn btn-lg btn-social-icon btn-facebook', title: "Authorize Facebook", rel: "nofollow" do %>
				<i class="fa fa-facebook"></i>
			<% end %>
			Connect
		<% end -%>
	</div>
	<div class="col-md-4">
		<% if @identities['twitter'] %>
			<%= link_to "#", class: 'btn btn-lg btn-social-icon btn-twitter disabled', title: "Authorize Twitter", rel: "nofollow" do %>
				<i class="fa fa-twitter"></i>
			<% end %>
			Connected
		<% else %>
			<%= link_to user_twitter_omniauth_authorize_path, class: 'btn btn-lg btn-social-icon btn-twitter', title: "Authorize Twitter", rel: "nofollow" do %>
				<i class="fa fa-twitter"></i>
			<% end %>
			Connect
		<% end -%>
	</div>
</div>
